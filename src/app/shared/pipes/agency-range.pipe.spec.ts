import { AgencyRangePipe } from './agency-range.pipe';

describe('AgencyRangePipe', () => {
  it('create an instance', () => {
    const pipe = new AgencyRangePipe();
    expect(pipe).toBeTruthy();
  });
});
